autoInstallPeers: false
# pnpm 10 skipped these build scripts silently; `false` keeps that behaviour (no native builds).
allowBuilds:
  bufferutil: false
  esbuild: false
  utf-8-validate: false
minimumReleaseAgeExclude:
  - '@lifi/*'
